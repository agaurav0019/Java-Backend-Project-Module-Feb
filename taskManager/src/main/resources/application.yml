server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:file:./test
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update # create dangerous for production data safest-> validate and update

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG